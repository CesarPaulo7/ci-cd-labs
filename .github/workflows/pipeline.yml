# name: Primer Pipeline CI

# on:
#   push:
#     branches:
#       - main

# jobs:
#   hello-ci:
#     name: Hello CI
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Mostrar información del entorno
#         run: |
#           echo "================================"
#           echo "      PRIMER PIPELINE CI"
#           echo "================================"
#           echo "Repositorio: ${{ github.repository }}"
#           echo "Rama: ${{ github.ref_name }}"
#           echo "Commit: ${{ github.sha }}"
#           echo "================================"

#       - name: Mostrar fecha y hora
#         run: date

#       - name: Mostrar versión de Git
#         run: git --version

#       - name: Finalizar pipeline
#         run: echo "Pipeline ejecutado correctamente."
name: Pipeline CI - Branching & PRs

on:
  push:
    branches:
      - main
      - 'feature/**'
  pull_request:
    branches:
      - main

jobs:
  hello-ci:
    name: Validar CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validar variables
        run: echo "Validando entorno..."

      # Paso de falla deliberada
      - name: Simular prueba corregida
        run: |
          echo "SUCCESS: La suite de pruebas automatizadas pasó correctamente."
          exit 0